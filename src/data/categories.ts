import {Category} from '../types/Category';

export const categories: Category = {
  input: {title: 'Entrada', color: 'green', expense: false},
  output: {title: 'Saída', color: 'red', expense: true},
}